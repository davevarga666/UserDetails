<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Widget.MaterialComponents.TextInputLayout.FilledBox.Dense">

        <item name="boxBackgroundColor">@android:color/white</item>
    </style>

    <style name="AppTextField" parent="Theme.MaterialComponents.Light.DarkActionBar">

        <item name="boxBackgroundColor">@android:color/white</item>
        <item name="textInputStyle">@style/AppTextField</item>

    </style>

</resources>